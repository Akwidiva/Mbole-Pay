# Helm values template for secrets (do NOT commit real values)
secrets:
  database:
    url: "postgresql://user:password@host:5432/mbole_pay"
  nextauth: "your-nextauth-secret"
  email:
    host: smtp.example.com
    port: 587
    user: you@example.com
    password: your-email-password

# You can reference these under your deployment manifests via template helpers
